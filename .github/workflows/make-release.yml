name: Make for system-monitor

on:
  workflow_run:
    workflows: ["CMake for system-monitor"]
    types: ["completed"]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    # - name: configure
    #   run: ./configure
    - name: make
      working-directory: ${{github.workspace}}/build
      run: make
    # - name: make check
    #   run: make check
    # - name: make distcheck
    #   run: make distcheck
